You are editing a specific field of a website.

Field path:
{{FIELD_PATH}}

Current value:
{{CURRENT_VALUE}}

User request:
{{USER_MESSAGE}}

Rewrite the field according to the request.

IMPORTANT RULES:
- Replace the current value with the improved text.
- Use the exact field path shown above.
- Do NOT return placeholders like {{FIELD_PATH}} or {{CURRENT_VALUE}}.
- Do NOT ask questions.
- Do NOT explain anything.
- Do NOT add text before or after the update block.
- Return ONLY the update block.
- The value must be valid JSON.

Return exactly in this format:

<update>
{
 "changes":[
   {
     "path":"{{FIELD_PATH}}",
     "value":"THE NEW TEXT HERE"
   }
 ]
}
</update>
