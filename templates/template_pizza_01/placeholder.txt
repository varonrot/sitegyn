<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ hero_title }}</title>
  <style>
    body { font-family: Arial; padding: 40px; }
    h1 { color: #d7263d; }
    p { font-size: 18px; max-width: 600px; }
    a.button {
      display: inline-block;
      padding: 10px 20px;
      background: #d7263d;
      color: white;
      text-decoration: none;
      margin-top: 20px;
    }
  </style>
</head>
<body>

  <h1>{{ hero_title }}</h1>
  <p>{{ hero_paragraph }}</p>
  <a href="#" class="button">{{ hero_cta }}</a>

</body>
</html>
